---
apiVersion: "external-secrets.io/v1"
kind: "ExternalSecret"
metadata:
  name: "media-postgres"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: "vault"
    kind: "ClusterSecretStore"
  target:
    name: "media-postgres"
    creationPolicy: "Owner"
  data:
    - secretKey: "username"
      remoteRef:
        key: "secret/soeren.cloud/env/prod/media/postgres"
        property: "POSTGRES_USER"
    - secretKey: "password"
      remoteRef:
        key: "secret/soeren.cloud/env/prod/media/postgres"
        property: "POSTGRES_PASSWORD"
