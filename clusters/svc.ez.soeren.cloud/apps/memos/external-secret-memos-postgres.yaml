---
apiVersion: "external-secrets.io/v1"
kind: "ExternalSecret"
metadata:
  name: "memos-postgres"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: "vault"
    kind: "ClusterSecretStore"
  target:
    name: "memos-postgres"
    creationPolicy: "Owner"
  data:
    - secretKey: "username"
      remoteRef:
        key: "secret/soeren.cloud/env/prod/memos"
        property: "POSTGRES_USER"
    - secretKey: "password"
      remoteRef:
        key: "secret/soeren.cloud/env/prod/memos"
        property: "POSTGRES_PASSWORD"
