---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "nats"
spec:
  ports:
    - port: 4222
      name: "nats"
      targetPort: "clients"
    - port: 80
      name: "nats-mgmt"
      targetPort: "management"
  selector:
    app.kubernetes.io/name: nats
