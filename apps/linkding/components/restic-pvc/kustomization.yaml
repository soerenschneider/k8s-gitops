---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ../../../../infra/unrest-pvc
patches:
  - target:
      kind: "CronJob"
      name: "unrest-backup-pvc"
    patch: |
      - op: "replace"
        path: "/spec/jobTemplate/spec/template/spec/containers/0/envFrom"
        value:
          - secretRef:
              name: "linkding-restic"
              optional: true
          - configMapRef:
              name: "linkding-restic"
              optional: true
      - op: "replace"
        path: "/spec/jobTemplate/spec/template/metadata/labels/restic~1name"
        value: "linkding"
      - op: "test"
        path: "/spec/jobTemplate/spec/template/spec/volumes/2/name"
        value: "data"
      - op: "replace"
        path: "/spec/jobTemplate/spec/template/spec/volumes/2/persistentVolumeClaim/claimName"
        value: "linkding"
