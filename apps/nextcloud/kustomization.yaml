---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: nextcloud-config
    envs:
      - nextcloud.properties
  - name: nginx-config
    files:
      - nginx.conf
