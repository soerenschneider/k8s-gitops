---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: glance-config
    behavior: create
    options:
      disableNameSuffixHash: true
    files:
      - glance-dd.yaml
      - glance-ez.yaml
      - glance-dev.yaml
